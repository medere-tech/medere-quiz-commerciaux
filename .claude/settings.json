{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "powershell -NoProfile -Command \"$i = $env:CLAUDE_TOOL_INPUT; if ($i -match 'git\\s+(commit|push|merge)') { Write-Error 'Les commits, push et merge sont reserves a Dethie.'; exit 2 } exit 0\""
          }
        ]
      }
    ]
  }
}