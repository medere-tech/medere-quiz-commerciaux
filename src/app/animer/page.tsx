import { SessionAnimateur } from '@/composants/session/SessionAnimateur';

/** 10b · Session collective, côté animatrice. Écran projeté. */
export default async function PageAnimation() {
  return <SessionAnimateur />;
}
